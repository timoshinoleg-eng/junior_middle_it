[project]
name = "junior-middle-it"
version = "0.1.0"
requires-python = ">=3.11,<3.15"
dependencies = [
  "python-telegram-bot==20.6",
  "python-dotenv==1.0.0",
  "requests==2.31.0",
  "telethon>=1.28.5",
  "PySocks>=1.7.1",
]

[tool.vercel]
entrypoint = "api.cron:handler"
