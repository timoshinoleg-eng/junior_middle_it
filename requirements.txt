# Core dependencies
python-telegram-bot[job-queue]==20.6
python-dotenv==1.0.0
requests==2.31.0

# Error tracking / observability
sentry-sdk>=2.0

# Growth P0: fuzzy cross-source dedup
rapidfuzz>=3.6.0

# Growth P1: durable profiles, referrals and product analytics
psycopg[binary]>=3.2,<4

# Growth P5: in-memory Resume Match document parsing
pypdf>=5.0,<7
python-docx>=1.1,<2

# Telegram channel parsing (optional)
telethon>=1.28.5
PySocks>=1.7.1

# Optional: для продвинутой обработки дат
# python-dateutil>=2.8.0
